<!-- GENERATED by scripts/gen_roadmap_board.py — do not edit -->
---
title: Roadmap
sidebar_position: 10
---

# Roadmap

What we are building, what is next, and — third on this page on purpose — what we are
deliberately not building. Most roadmap questions turn out to be that last one.

**There are no dates on this page.** Phase durations exist in our planning documents as
estimates. Published as commitments they would generate one kind of feedback and no useful
planning, so they are not published.

## What we are working on now

**Phase 11 — Interop, Not Lock-in** · goal G5

| Spec | What it is | Placement | Status |
|---|---|---|---|
| GRVX-1101 | Bare-Parquet access — query `data/warehouse/` with DuckDB, no Gravix process running | `core` | done |
| GRVX-1102 | Prometheus remote-write receiver → facts, with cardinality budget enforcement | `core` | done |
| GRVX-1103 | SQL-vs-PromQL guide + verified Metabase/Superset connection | `core` | blocked |
| GRVX-1104 | Grafana datasource plugin — keep Grafana, get Gravix correctness | `core` | blocked |
| GRVX-1105 | OTLP metrics-subset hardening — reject traces and logs at the receiver, accept a cardinality-budgeted metrics subset | `core` | done |
| GRVX-1106 | Apache Iceberg table format, readable by Spark and Trino | `core` | blocked |
| GRVX-1107 | Data export in the free tier — Parquet, CSV, JSONL, manual and scheduled | `core` | partial |
| GRVX-1108 | Import history from Prometheus TSDB and Datadog metric exports | `core` | partial |
| GRVX-1109 | The documented exit path, executed in CI | `core` | done |

### Blocked

Work that is specified and cannot move until something outside the code changes — a
decision, a credential, a piece of infrastructure, or a person. It is listed rather than
quietly omitted, because a roadmap that shows only what is moving is a roadmap that
cannot be checked.

| Spec | What it is | Phase |
|---|---|---|
| GRVX-1002 | Demonstrate cost immunity to cardinality | 10 |
| GRVX-1003 | Reach ≤120 bytes per event at 30-day retention | 10 |
| GRVX-1004 | Cost model and public TCO calculator, bootstrap and at-scale together | 10 |
| GRVX-1005 | Reach ≥20,000 events/sec/core ingest throughput | 10 |
| GRVX-1006 | Dashboard query p95 ≤400 ms warm, with an honest cold number | 10 |
| GRVX-1007 | Publish the benchmark page, regenerated by CI on every release | 10 |
| GRVX-1008 | A reproduction guide for running our benchmark on your own hardware | 10 |
| GRVX-1103 | SQL-vs-PromQL guide + verified Metabase/Superset connection | 11 |
| GRVX-1104 | Grafana datasource plugin — keep Grafana, get Gravix correctness | 11 |
| GRVX-1106 | Apache Iceberg table format, readable by Spark and Trino | 11 |
| GRVX-1304 | `ee/tenancy/` — the multi-tenant control plane | 13 |
| GRVX-1305 | `ee/billing/` — metering, Stripe, invoicing and overage | 13 |
| GRVX-1306 | `ee/identity/` — SAML, SCIM and directory sync (single-org OIDC stays free) | 13 |
| GRVX-1308 | `ee/compliance/` — SIEM streaming, retention holds, SOC 2 evidence | 13 |
| GRVX-1310 | `ee/whitelabel/` — branding, custom domains and embeds | 13 |
| GRVX-1312 | Pro packaging, pricing, and the public page that lists what stays free | 13 |
| GRVX-1401 | Prove Gravix Cloud runs the unmodified OSS core, by build provenance | 14 |

## What is next

**Phase 12 — The Community Machine** · goal G6

| Spec | What it is | Placement |
|---|---|---|
| GRVX-1201 | Plugin system v2 — a stable, versioned ABI for notifiers, exporters and adapters | `core` |
| GRVX-1202 | Plugin registry and `gravix plugin new` scaffold | `core` |
| GRVX-1203 | The contribution ladder — contributor, reviewer, maintainer | `core` |
| GRVX-1204 | The RFC process and public decision log | `core` |
| GRVX-1205 | The `good first issue` pipeline | `core` |
| GRVX-1206 | One-command dev setup and a contributor test suite under five minutes | `core` |
| GRVX-1207 | A public roadmap board fed by the goal loop | `core` |
| GRVX-1208 | Adopters, case studies, and the community call record | `core` |
| GRVX-1209 | Release notes that credit every contributor by name | `core` |
| GRVX-1210 | Grant merge rights to at least three non-founder maintainers | `core` |

## What we are deliberately not building

These are permanent. They are not a backlog, they are not waiting for demand, and a
well-argued request that crosses one is still declined — we will say so, cite the section,
and name the tool that does do it. Changing any of them needs the charter's §6 amendment
procedure, not a vote.

### §1. No Distributed Tracing

- We will not implement span collection, parent/child context propagation, or waterfall visualizations.
- We will not support OpenTelemetry trace ingestion.
- **Use instead:** **Jaeger** or **Grafana Tempo** if you self-host, **Honeycomb** if you would rather not. Tracing is a genuinely hard problem that these solve well, and half-solving it here would help nobody.

### §2. No Log Aggregation

- We will not build a log search engine (like ELK/Splunk).
- We will not ingest raw text logs or `stdout`/`stderr` streams.
- **Use instead:** **Grafana Loki** if you want something small, **OpenSearch** or **Elasticsearch** if you want search. Logs and metrics have different storage economics, and pretending otherwise is how observability bills get surprising.

### §3. No Agents

- We will not build, distribute, or support sidecar agents or host-level daemons.
- We will not collect system metrics (CPU, Memory, Disk) from hosts.
- **Use instead:** **Prometheus node_exporter** for host metrics, or **Telegraf** if you want an agent that collects many things. Both do this properly and neither needs us.

### §4. No Real-Time Dashboards

- We will not support sub-minute latency.
- We will not support streaming query engines.
- **Use instead:** **Prometheus** with a short scrape interval, or **Grafana Live** for a streaming panel. Sub-minute is an architecture, not a setting, and theirs is built for it.

### §5. No High-Cardinality Dimensions

- We will not index `user_id`, `request_id`, `session_id`, or `ip_address` as dimensions.
- **Use instead:** **ClickHouse** if you genuinely need per-user or per-request dimensions at scale. It is built for that cardinality and priced for it; we are neither.

### §6. No Custom Query Language

- We will not invent a Domain Specific Language (DSL) like PromQL or LogQL.
- **Use instead:** **Trino**, **DuckDB** or any SQL client, pointed at `data/warehouse/`. The files are plain Parquet and readable with no Gravix process running — see [Bare-Parquet access](../docs-site/docs/bare-parquet-access.md).

### §7. No Feature Parity with Datadog

- We will not attempt to clone Datadog/NewRelic features.
- We will not support complex alerting rules, anomaly detection, or APM features.
- **Use instead:** **Datadog**, **New Relic** or **Grafana Cloud**, and we mean that without irony. If you need the full platform, buy the full platform; Gravix is one honest instrument, and running both is a reasonable thing to do.

## Goals we considered and rejected

Recording these stops them being re-proposed every quarter as though they were new.

| Not a goal | Why |
|---|---|
| GitHub stars | Vanity. Correlates with launch-day attention, not usage. |
| Feature count vs Datadog | Charter §4. Parity is forbidden, and losing that race is the default outcome. |
| Sub-minute latency | Non-goal §4. Architectural, not aspirational. |
| Supporting every language | SDKs follow demand. Three cover most of it. |
| Being the only tool a team uses | We are one honest instrument, not a platform. Teams should also run tracing — from someone else. |
| Maximising OSS→Pro conversion | The anti-metric. High conversion with falling OSS retention means the free tier is being degraded. |
| Enterprise logos | Lagging indicator. Chasing it distorts the roadmap toward whoever is loudest. |

## What the community has asked for

Open a **Roadmap input** issue and tell us the *problem*. Reactions tell us what people want;
they do not decide what gets built — see
[how input is weighed](https://github.com/lgreene03/gravix-dashboards/blob/main/docs/oss/roadmap-input.md).

Every item carries a disposition, and nothing sits unlabelled. An item may sit
`under consideration` for at most 2 goal reviews; after that it
is accepted or declined with a reason, because indefinite consideration is a decline nobody
has to defend.

_No roadmap input yet._ Yours would be the first.

## How this page is made

Generated by `scripts/gen_roadmap_board.py` from
[`docs/oss/12-goal-tree.md`](https://github.com/lgreene03/gravix-dashboards/blob/main/docs/oss/12-goal-tree.md),
[`docs/oss/specs/SPEC-INDEX.md`](https://github.com/lgreene03/gravix-dashboards/blob/main/docs/oss/specs/SPEC-INDEX.md)
and
[`docs/04-non-goals.md`](https://github.com/lgreene03/gravix-dashboards/blob/main/docs/04-non-goals.md),
on every commit.

`make roadmap-check` fails when this page differs from a fresh generation, so a stale roadmap
is a red build rather than an oversight somebody discovers months later. Nothing on this page
is typed by hand; to change it, change the document it comes from.

